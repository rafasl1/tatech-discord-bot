export class SongPlayer {
    // todo: classe que vai ser responsável por gerenciar a queue de músicas
    constructor() {}

    async processSongMessage(songMessage: string) {
        console.log(`${songMessage}`)
    }
}